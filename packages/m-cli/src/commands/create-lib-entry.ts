import prompts from 'prompts';
import logger from '../shared/logger';
import { CreateCMD } from './create';

export default async function createLibEntryAction(args: string[] = [], options: CreateCMD = {}) {
  console.log('createLibEntryAction')
}